<% provide(:title, 'All classes') %>

<div class="row">
  <h1 class="center">Character Classes</h1>

  <div class="col-md-3">
    <h1>
      <small>Add Character Class</small>
    </h1>
    <%= render 'character_class_form' %>
  </div>

  <div class="col-md-9">
    <table class="character-class table table-striped table-bordered table-white" id="character-class-table" data-source="<%= classes_path(format: :json) %>">
      <thead>
        <th>Name</th>
        <th>Blizzard ID</th>
      </thead>
      <tbody>
      </tbody>
    </table>
  </div>
</div>
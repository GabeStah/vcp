<tr>
  <td><%= link_to raid.zone.name, raid %></td>
  <td><%= l raid.started_at %></td>
  <td><%= l raid.ended_at if raid.ended_at %></td>
  <% if current_user.present? %>
    <td>
      <%= render 'raid_edit', raid: raid %>
    </td>
  <% end %>
</tr>
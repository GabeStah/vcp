<li>
  <%= link_to guild.name, guild %>
  <% if current_user && current_user.admin? %>
    | <%= link_to 'delete', guild, method: :delete,
                  data: { confirm: 'You sure?' } %>
  <% end %>
</li>